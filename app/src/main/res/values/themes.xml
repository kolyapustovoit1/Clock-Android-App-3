<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Clock" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- SplashScreen Theme -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/backgroundColor</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/clock</item>
        <item name="postSplashScreenTheme">@style/Theme.Clock</item>
    </style>
</resources>